<!DOCTYPE html>
<html>
<head>
    <title>Balance Report</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
        th, td {
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .bold {
            font-weight: bold;
            background-color: #e0e0e0;
        }
    </style>
</head>
<body>
    <h2>Balance  Report</h2>
    <table>
        <thead>
            <tr>
                <th>client Name</th>
                <th>Credit</th>
                <th>Debit</th>
            </tr>
        </thead>
        <tbody>
            {{#each data}}
            <tr>
                <td 
                    {{#if (eq party_name "Total")}}class="bold"{{/if}}>
                    {{party_name}}
                </td>
                <td 
                    {{#if (eq party_name "Total")}}class="bold"{{/if}}>
                    {{credit}}
                </td>
                <td 
                    {{#if (eq party_name "Total")}}class="bold"{{/if}}>
                    {{debit}}
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>